name: Continuous Integration

on:
  push:
    branches:
      - main
      - master
      - develop
  pull_request:
    branches:
      - main
      - master

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-matrix:
    name: Test (${{ matrix.os }} - Python ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
        python-version:
          - "3.9"
          - "3.10"
          - "3.11"
          - "3.12"
          - "3.13"

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"
          cache-dependency-path: |
            pyproject.toml
            setup.py
            requirements*.txt

      - name: Upgrade Core Packaging Tools
        run: |
          python -m pip install --upgrade pip setuptools wheel build twine

      - name: Install Dependencies and Package
        run: |
          python -m pip install -e .[test,dev] || python -m pip install pytest pytest-cov pyyaml ruff flake8

      - name: Code Quality & Linting (Ubuntu only)
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
        run: |
          ruff check src tests || true
          flake8 src tests --max-line-length=120 --ignore=E203,W503 || true

      - name: Run Test Suite with Pytest
        env:
          PYTHONPATH: src
        run: |
          pytest tests -v --durations=10

      - name: Build Distribution Packages
        run: |
          python -m build

      - name: Verify Package Metadata & Integrity
        run: |
          twine check dist/*
